#include <stdio.h>

int main(int argc, char *argv[]) {
  int key = getchar();
  printf("YOU HAVE PRESS KEY: '%c'(%d)\n", key, key);
  key = getchar();
  printf("YOU HAVE PRESS KEY: '%c'(%d)\n", key, key);
  key = getchar();
  printf("YOU HAVE PRESS KEY: '%c'(%d)\n", key, key);
  return 0;
}
